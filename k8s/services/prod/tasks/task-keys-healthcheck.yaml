apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-keys-healthcheck
  namespace: vpn-prod
  labels:
    app: task-keys-healthcheck
spec:
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: task-keys-healthcheck
  template:
    metadata:
      labels:
        app: task-keys-healthcheck
    spec:
      containers:
      - name: task-keys-healthcheck
        image: ghcr.io/kelvpn/vpn4-prod:latest
        args: [ "keys_healthcheck" , "10" ]
        env:
          - name: PYTHONUNBUFFERED
            value: "1"
        envFrom:
          - secretRef:
              name: app-secrets-prod
      imagePullSecrets:
        - name: github-registry-secret
